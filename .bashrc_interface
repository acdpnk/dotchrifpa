# functions
	
	function jobnumber {
		if [ $(jobs | grep -c -e "[0-9]") != "0" ]; then
			echo -n '['
			echo -n $(( $(jobs | grep -c -e "[0-9]") - $(jobs | grep -c Done) - $(($(jobs | grep -c Terminated)*2)) ))
			echo -n '] '
		fi
	}

	function isgit {
		echo $(git rev-parse --git-dir 2>&1 | grep -c fatal)
	}

	function gitisclean {
		if [ $(isgit) == "0" ] && [ $(git status 2>&1 | grep -c clean) == "0" ]; then
			echo •
		fi
	}

	function gitbranch {
		if [ $(isgit) == "0" ]; then
			echo -n ' on '
			git rev-parse --abbrev-ref HEAD 2>/dev/null
		fi
	}

	function promptsign {
		if [ $(isgit) == "0" ]; then
			echo ¥
		else
			echo $
		fi
	}

# prompt
	PS1='$(jobnumber)[\u@\h \W$(gitbranch)$(gitisclean)]$(promptsign) '


# environmental stuff
	set -o vi
