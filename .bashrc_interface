# functions

function gitbranch {
	if [ -e .git ]; then
		echo -n 'on '; git rev-parse --abbrev-ref HEAD
	fi
}

function promptsign {
	if [ -e .git ]; then
		echo ¥
	else
		echo $
	fi
}

# prompt

PS1='[$(date +%H:%M)] [\u@\h \W $(gitbranch)]$(promptsign) '


# environmental stuff

set -o vi